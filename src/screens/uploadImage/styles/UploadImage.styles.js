import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   
    buttonContainer: {
        marginVertical: 16,
        width: '100%',
    },
    imageContainer: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        rowGap: 8,
        columnGap: 8
    },
    image: {
        width: 125,
        height: 125,
        borderRadius: 8,
    },
});
